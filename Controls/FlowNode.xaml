<UserControl x:Class="FlowDiagram.Controls.FlowNode"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Ellipse x:Name="NodeBackground" Width="50" Height="50" Fill="Gray" />
        <TextBlock Text="{Binding NodeName}" HorizontalAlignment="Center" VerticalAlignment="Center" />
        <Ellipse x:Name="HighlightEffect" Width="60" Height="60" Stroke="Yellow" StrokeThickness="2"
                 Visibility="Collapsed">
            <Ellipse.RenderTransform>
                <ScaleTransform x:Name="EffectTransform" ScaleX="1" ScaleY="1" CenterX="30" CenterY="30" />
            </Ellipse.RenderTransform>
        </Ellipse>
    </Grid>
</UserControl>